<?php

return [

    /*
    |--------------------------------------------------------------------------
    | Admin IP Whitelist
    |--------------------------------------------------------------------------
    |
    | Comma-separated list of IP addresses allowed to access the admin panel.
    | If empty, no IP restriction is applied (backwards compatible).
    |
    */

    'admin_ip_whitelist' => env('ADMIN_IP_WHITELIST', ''),

];
